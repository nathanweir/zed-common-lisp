; (
;   (source
;     [
;       (defvar)
;       (defparameter)
;       (defconstant)
;       (defmacro)
;       (defun)
;       (defmethod)
;       (defgeneric)
;       (lambda)
;       (let)
;       (destr_bind)
;       (defclass)
;       (with_slots)
;       (labels)
;       (flet)
;       (macrolet)
;       (in_package)
;       (list)
;     ] @run @CL_EVAL_EXPRESSION
;   )
;   (#set! tag cl-eval-expression)
; )
